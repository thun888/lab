name: get
on: 
  schedule:
    - cron: '0/15 * * * *'
  push:
    branches: 
      - main

jobs:
  download:
    runs-on: ubuntu-latest

    steps:
    - name: install GO
      uses: actions/setup-go@v2.1.4
      
    - name: run it
      run: |
        go run main.go
